{
  "name": "bookiebuddy-service",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "npm run build && func start -p 2000",
    "test": "echo \"No tests yet...\"",
    "clean": "rm -r dist"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "16.x",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "@types/json-schema": "^7.0.11",
    "@types/jsonwebtoken": "^8.5.9",
    "azure-middleware": "^1.0.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.9.1"
  }
}
